<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/no_internet_dialog_bg">

    <!-- WiFi Off Icon -->
    <ImageView
        android:id="@+id/ivNoInternet"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/ic_no_internet"
        android:contentDescription="No Internet"
        app:tint="@color/profile_icon_email" />

    <!-- Title -->
    <TextView
        android:id="@+id/tvNoInternetTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="20dp"
        android:text="No Internet Connection"
        android:textColor="@color/profile_text_primary"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Message -->
    <TextView
        android:id="@+id/tvNoInternetMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:text="Please check your internet connection and try again"
        android:textColor="@color/profile_text_secondary"
        android:textSize="14sp" />

    <!-- Animated WiFi Indicator (Optional) -->
    <ProgressBar
        android:id="@+id/pbConnecting"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="20dp"
        android:indeterminateTint="@color/profile_icon_email"
        android:visibility="gone" />

</LinearLayout>
